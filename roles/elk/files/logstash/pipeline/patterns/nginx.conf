NGINXACCESS %{WORD:service} %{IPORHOST:remote_ip} %{USER:ident} %{USER:auth} \[%{HTTPDATE:timestamp}\] "(?:%{WORD:verb} %{URIPATHPARAM:request}(?: HTTP/%{NUMBER:httpversion})?|-)" %{NUMBER:response} (?:%{NUMBER:bytes}|-) "(?:%{URI:referrer}|-)" %{QS:agent} (?:%{NUMBER:request_time}|-) (?:%{NUMBER:upstream_response_time}|-) (?:%{NUMBER:gzip_ratio}|-) (?:%{WORD:cache_hit}|-)
NGGREED %{IPORHOST:remote_ip} %{USER:ident} %{USER:auth} \[%{HTTPDATE:timestamp}\] "%{GREEDYDATA:stdout}" %{NUMBER:response} (?:%{NUMBER:bytes}|-) "(?:%{URI:referrer}|-)" %{QS:agent} (?:%{NUMBER:request_time}|-) (?:%{NUMBER:upstream_response_time}|-) (?:%{NUMBER:gzip_ratio}|-) (?:%{WORD:cache_hit}|-)
NGGRALL %{IPORHOST:remote_ip} %{USER:ident} %{USER:auth} \[%{HTTPDATE:timestamp}\] "(?:%{WORD:verb} %{NOTSPACE:request}(?: HTTP/%{NUMBER:httpversion})?|-)" %{NUMBER:response} (?:%{NUMBER:bytes}|-) "(?:%{NOTSPACE:referrer}|-)" %{QS:agent} (?:%{NUMBER:request_time}|-) (?:%{NUMBER:upstream_response_time}|-) (?:%{NUMBER:gzip_ratio}|-) (?:%{WORD:cache_hit}|-)
SERVICE %{WORD:service}