# В случае возникновения ошибок, нужно правильно удалить mysql-server:
#
# sudo  rm -rf /var/lib/mysql*
# sudo rm -rf /var/log/mysql
# sudo rm -rf /etc/mysql
# sudo rm -rf /var/lib/mysql
# sudo apt remove mysql-server* -y && sudo apt purge mysql-server* -y && sudo apt autoremove -y
